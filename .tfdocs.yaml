formatter: "markdown table"

sections:
  hide:
    - resources
    - requirements

output:
  file: ./docs/source/TFDOCS.md
  mode: inject

content: |-
  {{ .Header }}

  {{ .Modules }}

  {{ .Inputs }}

  {{ .Outputs }}  